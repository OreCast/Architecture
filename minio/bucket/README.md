# MinIO test repository

```
# to build
go build

# config
{
    "endpoint": "localhost:9000",
    "access_key": "xyz",
    "secret": "xyz",
    "uzeSSL": false,
    "verbose": 1
}

# run
./miniobucket -config config.json
```
